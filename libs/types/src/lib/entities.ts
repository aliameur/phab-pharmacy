export type TTestimonial = {
  id: string;
  name: string;
  testimonial: string;
  imageSrc: string;
};

export type TTeamMember = {
  id: string;
  name: string;
  title: string;
  imageSrc: string;
  // TODO figure out social links
};
